{% extends 'base_sin_menu.html.twig' %}
{% block content %}
    <br>
    <div class="panel panel-default">
        <div class="panel-heading">
            <h4>Configuración de la entidad</h4>
        </div>
    </div>

    <ul class="nav nav-tabs">
        <li class="active"><a href="#lista" data-toggle="tab">Lista</a></li>
        <li><a href="#excel" data-toggle="tab">Excel</a></li>
        <li><a href="#filtros" data-toggle="tab">Filtros</a></li>
    </ul>
    <div class="tab-content">
        <div class="tab-pane active" id="lista">
            <!-- Menu Encabezado Movimiento -->
            <div id="lista" style="width: 100%"><br>
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                    <tr>
                        <th>CAMPO</th>
                        <th>ALIAS</th>
                        <th></th>
                    </tr>
                    </thead>
                    <tbody>
                    {{ form_start(form) }}
                    {% for arColumna in arrColumnasLista %}
                        <tr>
                            {% if isPk(arColumna.campo) %}
                                <td style="text-align: center">{{ splitByUnderscore(arColumna.campo) }}</td>
                                <td style="text-align: center"><input type="text" name="aliasLista[{{ arColumna.campo }}]"
                                                                      value="ID" readonly></td>
                                <td style="text-align: center"><input type="checkbox"
                                                                      name="mostrarLista[{{ arColumna.campo }}]" checked>
                                </td>
                            {% else %}
                                <td style="text-align: center">{{ splitByUnderscore(arColumna.campo) }}</td>
                                <td style="text-align: center"><input type="text" name="aliasLista[{{ arColumna.campo }}]"
                                                                      onkeypress="return validarCaracteres(event)"
                                                                      value="{{ arColumna.alias }}"></td>
                                <td style="text-align: center"><input type="checkbox"
                                                                      name="mostrarLista[{{ arColumna.campo }}]" {{ isSelected(arColumna.mostrar) }}>
                                </td>
                            {% endif %}
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        <div class="tab-pane active" id="excel">
            <div id="excel" style="width: 100%"><br>
            </div>
        </div>
    </div>
    <div style="float: right">
        {{ form_widget(form.btnGuardar) }}
        {{ form_end(form) }}
    </div>
    <style type="text/css">
        html {
            overflow-x: hidden;
        }
    </style>
{% endblock %}

