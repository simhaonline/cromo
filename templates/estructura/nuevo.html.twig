{% extends arConfiguracionEntidad.base %}
{% block content %}
    <div class="row">
        <div class="col-lg-12">
            {{ crearTitulo(arConfiguracionEntidad,3) }}
        </div>
    </div>
    <br>
    <div class="panel panel-default">
        <div class="panel-heading">
            Información general
        </div>
        <div class="panel-body">
            {{ form_start(form) }}
            {{ form_end(form) }}
        </div>
    </div>
    <div style="float: left;">
        <a class="btn btn-sm btn-default" href="{{ path('listado',{'entidad':arConfiguracionEntidad.codigoConfiguracionEntidadPk }) }}">Volver</a>
    </div>
{% endblock %}