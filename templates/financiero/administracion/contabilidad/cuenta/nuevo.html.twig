{% extends 'base.html.twig' %}
{% block content %}
    {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
    <div class="panel panel-default">
        <div class="panel-heading">Información general</div>
        <div class="panel-body">
            <div class="form-group form-group-sm">
                <label for="form_identificacionRel" class="col-sm-2 control-label">Codigo(Cuenta):</label>
                <div class="col-sm-4">
                    {{ form_widget(form.codigoCuentaPk) }}
                </div>
                <label for="form_numeroIdentificacion" class="col-sm-2 control-label">Nombre:</label>
                <div class="col-sm-4">
                    {{ form_widget(form.nombre) }}
                </div>
            </div>
            <div class="form-group form-group-sm">
                <label for="form_identificacionRel" class="col-sm-2 control-label"></label>
                <div class="col-sm-10">
                    {{ form_widget(form.exigeTercero) }}
                    {{ form_widget(form.exigeCentroCosto) }}
                    {{ form_widget(form.exigeBase) }}
                    {{ form_widget(form.permiteMovimiento) }}
                </div>

            </div>
        </div>
    </div>
    <div class="modal-footer">
        <a class="btn btn-default btn-sm" href="{{ path('financiero_administracion_contabilidad_cuenta_lista') }}">Cancelar</a>
        {{ form_widget(form.guardar, { 'attr': {'class': 'btn btn-primary btn-sm'} }) }}
    </div>
    {{ form_end(form) }}
{% endblock %}
