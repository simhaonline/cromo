{% extends 'base_general.html.twig' %}
{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <h3 class='page-header'>
                General seguridad
                <small style="font-size: 20px">usuario: lista</small>
            </h3>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    {{ form_start(form) }}
    <div>
        <table class="table table-striped table-bordered table-hover">
            <thead>
            <tr>
            </tr>
            </thead>
            <tbody>
            {% for arLog in arLogs %}
                <tr>
                    <td>{{ arLog.codigoLogPk }}</td>
                    {#<td style="text-align: center;">#}
                        {#<a href="{{ path('gen_seguridad_usuario_nuevo', {'hash' : encriptar( arUsuario.id)}) }}">#}
                            {#<i class="fa fa-edit" style="font-size: large">#}
                            {#</i>#}
                        {#</a>#}
                    {#</td>#}
                    {#<td style="text-align: center;">#}
                        {#<a class="btn btn-xs btn-default" title="Cambiar contraseña"#}
                           {#href="javascript:abrirVentana3('{{ path('gen_seguridad_usuario_nuevo_clave',{'hash' : encriptar(arUsuario.id)}) }}','cambiarContrasena',600,900)">#}
                            {#<i class="fa fa-key" style="font-size: smaller;color: red;">#}
                            {#</i>#}
                        {#</a>#}
                    {#</td>#}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="btn-toolbar">
        <div style="float: right;" class="btn-group">
        </div>
        <div style="float: right;" class="btn-group">
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}
