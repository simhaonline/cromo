{% extends 'base.html.twig' %}
{% block content %}
    {{ form_start(form, { 'attr': {'class': 'form-horizontal', 'novalidate': 'novalidate'} }) }}
    {{ form_errors(form) }}
    <div class="panel panel-default">
        <div class="panel-heading">Filtros</div>
        <div class="panel-body">
        </div>
        <div class="modal-footer">
            {{ form_widget(form.btnFiltrar) }}
        </div>
    </div>
    <table class="table table-striped table-bordered table-hover">
        <thead>
        <tr>
            <th>ID</th>
            <th>USUARIO</th>
            <th>BODEGA</th>
        </tr>
        </thead>
        <tbody>
        {% for arBodegaUsuario in arBodegaUsuario %}
            <tr>
                <td>{{ arBodegaUsuario.codigoBodegaUsuarioPk }}</td>
                <td>{{ arBodegaUsuario.usuario }}</td>
                <td>{{ arBodegaUsuario.bodega }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="btn-group" style="float: left">
        <div class="btn-group" style="float: left">
            <div class="btn btn-default btn-sm">Registros <span class="badge">{{ arBodegaUsuario.getTotalItemCount }}</span>
            </div>
        </div>
    </div>
    <div class="btn-group btn-xs" style="float: left;margin-left: 0px;padding-top: 0px;">
        {{ knp_pagination_render(arBodegaUsuario) }}
    </div>

    <div class="btn-toolbar" style="float: right">
        <div class="btn-group">
            {#{{ form_widget(form.btnExcel, { 'attr': {'class': 'btn btn-default btn-sm', 'style':'float:right'} }) }}#}
        </div>
        <div class="btn-group">
            <a class="btn btn-default btn-sm"
               href="{{ path('inventario_administracion_inventario_bodegausuario_nuevo', {'id': 0}) }}">Nuevo</a>
        </div>
    </div>
    <br><br><br>
    {{ form_rest(form) }}
    {{ form_end(form) }}
{% endblock %}

