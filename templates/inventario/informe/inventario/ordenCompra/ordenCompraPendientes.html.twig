{% extends 'base_inventario.html.twig' %}
{% block content %}
    {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
    <div class="panel panel-default">
        <div class="panel-heading">
            Filtros
        </div>
        <div class="panel-body">
            <div class="form-group form-group-sm">
                <label for="form_solicitudTipoRel" class="col-sm-2 control-label">Tipo:</label>
                <div class="col-sm-4">
                    {{ form_widget(form.ordenCompraTipoRel) }}
                    {{ form_errors(form.ordenCompraTipoRel) }}
                </div>
                <label for="form_txtNumeroOrdenCompra" class="col-sm-2 control-label">Numero oc:</label>
                <div class="col-sm-4">
                    {{ form_widget(form.txtNumeroOrdenCompra) }}
                    {{ form_errors(form.txtNumeroOrdenCompra) }}
                </div>
            </div>
        </div>
        <div class="modal-footer">
            <div class="btn-group">
                {{ form_widget(form.btnFiltrar) }}
            </div>
        </div>
    </div>
    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover">
            <thead>
            <tr>
                <th>ID</th>
                <th title="CODIGO ITEM">CODIGO</th>
                <th title="NUMERO ORDEN DE COMPRA">NUMERO</th>
                <th>NOMBRE</th>
                <th>EXISTENCIAS</th>
                <th>STOCK_MIN</th>
                <th>STOCK_MAX</th>
                <th>CANTIDAD SOLICITADA</th>
                <th>CANTIDAD PENDIENTE</th>
                <th>CANTIDAD A ORDENAR</th>
            </tr>
            </thead>
            <tbody>
            {% for arOrdenCompraDetalle in arOrdenCompraDetalles %}
                <tr>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.codigoOrdenCompraDetallePk }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.codigoItem }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.ordenCompra }}</td>
                    <td style="text-align: left;">{{ arOrdenCompraDetalle.nombre }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.cantidadExistencia }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.stockMinimo }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.stockMaximo }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.cantidad }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.cantidadPendiente }}</td>
                    <td style="text-align: right;">{{ arOrdenCompraDetalle.cantidad }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="btn-toolbar" style="float: left">
        <div class="btn-group" style="float: left">
            <div class="btn btn-default btn-sm">Registros <span
                        class="badge">{{ arOrdenCompraDetalles.getTotalItemCount }}</span>
            </div>
        </div>
    </div>
    <div class="btn-group" style="float: right;">
        {{ form_widget(form.btnExcel) }}
    </div>
    <div class="btn-group btn-xs">
        {{ knp_pagination_render(arOrdenCompraDetalles) }}
    </div>
    {{ form_end(form) }}
{% endblock %}
