{% extends 'base.html.twig' %}
{% block content %}
    <div class="btn-toolbar" style="float:left">
        <div class="btn-group">
            <a class="btn btn-default btn-sm"
               href="{{ path('recursohumano_administracion_nomina_embargojuzgado_lista') }}">Volver</a> &nbsp;&nbsp;
        </div>
    </div>
    <br><br>
    <table width="100%" class="table table-condensed table-bordered tabla-informacion">
        <tbody>
        <tr>
            <td class="titulo">ID:</td>
            <td class="td-in-well">{{ arEmbargoJuzgado.codigoEmbargoJuzgadoPk }}</td>
            <td class="titulo">CLIENTE:</td>
            <td class="td-in-well">{{ arEmbargoJuzgado.nombre|default("") }}</td>
            <td class="titulo">CLIENTE:</td>
            <td class="td-in-well">{{ arEmbargoJuzgado.oficina|default("") }}</td>
            <td class="titulo">CLIENTE:</td>
            <td class="td-in-well">{{ arEmbargoJuzgado.cuenta|default("") }}</td>

        </tbody>
    </table>
{% endblock %}
