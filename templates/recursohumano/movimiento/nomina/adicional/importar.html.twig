{% extends "base_sin_menu.html.twig" %}
{% block content %}
    {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
    <div class="panel panel-default">
        <div class="panel-heading">Cargar archivo</div>
        <div class="panel-body">
            <div>
                <div class="form-group form-group-sm">
                    <label for="form_numeroIdentificacón" class="col-sm-2 control-label">Cargar:</label>
                    <div class="col-sm-10">
                        {{ form_widget(form.attachment, { 'attr': {'class': 'btn btn-sm btn-default'} }) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="modal-footer">
        {{ form_widget(form.BtnCargar, { 'attr': {'class': 'btn btn-sm btn-primary'} }) }}
    </div>
    <blockquote>
        <p>Tener presenta la estructura que se requiere para subir el archivo de excel:</p><br>
        <footer><cite title="Source Title">El archivo de excel solo debe tener una hoja de calculo con el nombre hoja1</cite></footer>
        <footer><cite title="Source Title">El campo concepto es el codigo del pago concepto que se esta cargando masivamente.</cite></footer>
        <footer><cite title="Source Title">El numero de identificacion no debe tener puntos.</cite></footer>
        <footer><cite title="Source Title">Los valores no deben contener simbolos o caracteres, tampoco debe contener puntos (-+$).</cite></footer>
        <footer><cite title="Source Title">El campo detalle es un campo libre para agregar observaciones de la adicional al pago.</cite></footer><br>
    </blockquote>
    <br/><br/><br/><br/>
    {{ form_end(form) }}
{% endblock %}